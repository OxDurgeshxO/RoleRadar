import { CompareView } from "@/components/compare-view";

export const metadata = {
  title: "Compare Resumes — RoleFit",
  description: "Compare two versions of your resume to see skill and score improvements.",
};

export default function ComparePage() {
  return <CompareView />;
}
